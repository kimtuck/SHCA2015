app.directive('logo', [
    function () {
        return {
            templateUrl: "/UI/Directives/Logo/Logo.html",
            link:
                function (scope, element, attrs) {
            }
        };
    }
]);
